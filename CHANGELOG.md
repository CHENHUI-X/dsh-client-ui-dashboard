# Changelog

## 0.2.0 (2026-08-17)

### 新增
- **看板自持内部滚动容器**:根元素声明 `data-conversation-composer-overlay`(官方模式,轨迹视图同款),看板不再与「对话」「轨迹」共享会话列滚动体,切换视图不再被对话的挂载滚底带跑位置
- **按会话记忆滚动位置**:模块级 `Map<sessionId, scrollTop>` 记忆,多会话互不干扰;仅手动滚动写入,程序性恢复不回写;刷新/重启后回到顶部(符合预期)
- 底部预留 composer 高度,最后一行滚动到底不被输入框遮挡

> 感谢 [@fsvov](https://github.com/fsvov) 的贡献(PR #1),已加入 README 贡献者名单。

## 0.1.3 (2026-08-14)

### 修复
- **模型成本回归**:`modelSplit.costUsd` 从未累加恒为 0,按模型成本堆叠图与模型行成本列为死功能 → `RequestSample.costUsd` 同源计算,模型行/成本图/详情面板三处成本一致
- 工具计数两套规则统一(直方图与明细递归展开嵌套 subCalls,深度 ≥2 不再漏计)
- 趋势头部 TTFT 均值与耗时合计改用窗口口径(原全量值冒充窗口,标注误导)
- noProgress 对未上报 usage 的请求不再误报"无进展"
- stuck 告警拆出动态计时(live region 不再每秒打断读屏)
- "有新请求"横幅改按最新 seq 判断,筛选/搜索变化不再误报
- 模型时间线 hint 文案精简 + 换行兜底;模型行 sub 独立成行,长文本不再溢出卡片
- 图例名/压缩轮次列表/StatCard sub 溢出与截断兜底;tooltip 阴影改用存在的 `--dsw-shadow-lv2`
- 分组视图每组限 20 行 + 空态三态 + "按错误"视图只聚错误请求

### 新增
- 图表无障碍:四类图表组件本地化 aria-label,模型时间线仅切换点/最新点可聚焦
- 概览"平均首字延迟"补"全量"标注;TTFT 关联失效时给出提示
- 模型时间线基线轨;详情面板 tag 按语义着色;reasoning 统计值专属配色

### 改进
- 视图切换改语义化按钮(aria-pressed),表格/筛选组 aria-label 修正
- 成本/耗时等金额统一自适应格式;对比度加固(axisHint/modelNote/gaugeLabel)

## 0.1.2 (2026-08-14)

### 文档
- README 补三张看板截图(总览 / 耗时·工具·模型 / 请求明细),采用 GitHub 托管绝对链接,修复 npm 页截图无法显示的问题
- 新增 CHANGELOG 与发布流程说明(`RELEASE.md` + `scripts/release.mjs` 发布准备脚本)


所有版本的变更记录,按时间倒序。版本号遵循语义化版本(SemVer):`feat` → minor,`fix` → patch。

## 0.1.1 (2026-08-14)

### 修复
- 每模型平均 TTFT 关联键错误(`startSeq` → `resultSeq`),此前该功能恒为 null,已复活
- 成本估算定价档位改按**每条请求自身模型**计价,混合模型会话不再系统性算错
- 推理占比改为窗口 ÷ 窗口口径,不再系统性偏低
- 直方图最后一个桶闭区间,最大值样本不再漏计
- 详情面板"跳转轨迹"按钮被 24×24 方形规则压扁(重复 CSS),已拆分修复
- 模型横条与模型时间线双调色板不一致,统一为共享 `modelColor` 哈希
- 导出 JSON 忽略搜索词(所见非所得),现尊重筛选 + 搜索,payload 写入 view
- 模型切换计数与时间线对未知模型(null)处理不一致,统一为跳过
- 无进展行单位 tokens 误标为"字符"
- `deriveMetrics` 内部 `Date.now()` 破坏纯函数性,改为参数注入

### 新增
- **TTFT 命中 vs 未命中对比**(Timing 卡)
- **单请求估算成本**(详情面板 + 模型卡按模型成本堆积,金额自适应格式)
- **上下文占用率趋势图**(每请求输入 ÷ 上下文窗口,压缩回落可见)
- **错误数趋势图**(分钟桶)
- 筛选胶囊与视图 Tab **实时计数徽标**
- 全部趋势小图右上角**最大值标签**

### 改进
- 轮次耗时:窗口截断/进行中的轮次不再静默丢弃,带 `~` 近似标记 + 说明
- 模型时间线降采样(最多 60 点)+ "旧 → 新"方向标注 + 文案说人话
- 环形图单段整圆;直方图基线 + 0 桶不渲染;面积图边缘 tooltip 垂直位移保留
- 分组视图补列头、折叠状态按视图隔离;空态区分搜索/筛选并一键清除
- 无障碍:时间线焦点环、aria 本地化、hint "?" 语义、流式行不再每秒播报
- 溢出清理:StatCard 长数字紧凑化、组名/统计值截断防护、卡片 hint 换行
- 🔁/🐢 彩色 emoji 换成主题化文本徽标;running 状态统一为蓝

### 移除
- 耗时 × tokens 散点图(点击跳转)

## 0.1.0 (2026-08-14)

初始发布:DSH 浏览器端"看板"插件。实时会话监控页签,手写 SVG 图表(零第三方依赖)、CSS 变量主题、中英双语:

- 总览:轮次/步骤/请求/工具/压缩/成本等统计卡 + sparkline + Sticky 汇总条
- Token:输入/缓存读/缓存写/输出 + 推理拆分的环形图与堆叠条
- 上下文:压力仪表盘(半圆,阈值变色)+ 构成 + 注入来源
- 消息构成环形图、耗时分布(直方图 + P50/P95/P99)、TTFT 分位数
- 工具调用 TOP/耗时 TOP/失败率、错误分类、命令统计
- 模型对比 + 模型切换时间线、重试等待、失败画像
- 压缩效果(前后对比 + 回收条目)、推理强度分布
- Agent 健康诊断:循环检测、无进展、工具风暴密度
- 请求趋势:命中率/输入/输出/耗时/TTFT/每轮输入/推理占比/缓存写 8 图 + 密度图
- 请求明细:视图 Tab(表格/按轮次/按模型/按错误,分组可折叠)、状态筛选、实时搜索、JSON 导出、翻页、行展开、点击跳轨迹
